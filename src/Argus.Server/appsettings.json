{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Argus": {
    "//Password": "Unlocks the viewer. This file is tracked by git, so anything written here stays in the history - set ARGUS_PASSWORD in the environment instead to keep the real one out. Empty leaves the agent open to anyone who can reach the port.",
    "Password": ""
  }
}
